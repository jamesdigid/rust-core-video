These are Rust bindings to the Core Video framework on Mac OS X.

Currently, the available functionality is tailored to Servo's needs, but more APIs can be added if
they prove useful in other projects.

